# An isogram is a word that has no duplicate letters. Create a function that takes a string and
# returns either True or False depending on whether or not

def is_isogramic(x):
    result=False
    for i in x:
        if x.count(i)>1:
            result = True
        return result

print(is_isogramic("jayesh"))
